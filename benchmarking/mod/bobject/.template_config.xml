<?xml version="1.0" encoding="UTF-8"?>
<workload name="{{rw}}{{config_middle}}{{size}}_{{workers}}w" description="{{description}}">
<storage type="swift" config="timeout=300000" />
<auth type="swauth" config="username=cosbench:operator;password=intel2012;url=http://{{cluster_ip}}/auth/v1.0" />
<workflow>
<workstage name="{{main}}">
<work name="{{size}}" workers="{{workers}}" rampup="90" runtime="300" rampdown="30">
<operation type="{{rw}}" ratio="100" config="containers=u(1,100);objects=u(1,100);cprefix={{size}}-{{rw}}"/>
</work>
</workstage>
</workflow>
</workload>
